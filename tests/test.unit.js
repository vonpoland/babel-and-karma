import {imBigLogic} from '../public/js/logic';

describe('Date functions', function() {
    it('should return original date when executing dateFromString and dateToString', function() {
        expect(1).toEqual(imBigLogic());
    });
});